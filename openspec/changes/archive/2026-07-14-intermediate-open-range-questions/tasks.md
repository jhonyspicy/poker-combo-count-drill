## 1. 設定とヘルパーの追加

- [x] 1.1 `src/config/gameConfig.ts` に中級編で使うレンジ id 定数（初期値 `'open-range'`）を追加する
- [x] 1.2 `src/lib/presetRanges.ts` に id からプリセットを引く `getPresetRange(id)` を追加する（見つからない場合は例外）

## 2. 中級問題生成の変更（src/lib/questions.ts）

- [x] 2.1 オープンレンジのハンドタイプから自分のハンドを選び、スートをランダム割当で具体カード2枚に展開してから残りデッキでボードを配る処理を実装する（現行 `dealCards` の中級での利用を置き換え）
- [x] 2.2 出題対象ハンドの候補フィルタを実装する: オープンレンジ内 かつ デッドカードにそのハンドのランクが1枚以上含まれる かつ `expandHandCombos` の残りコンボ数が1以上
- [x] 2.3 候補からの抽選を実装する: カテゴリ（pair 25% / suited 25% / offsuit 50%）を候補が空でないカテゴリの中から重み付きで選び、カテゴリ内は一様に選ぶ
- [x] 2.4 `makePairQuestion` / `makeSuitedQuestion` / `makeOffsuitQuestion` を「選ばれたハンドタイプを受け取る」形に変更する（答え・解説・誤答生成は現行ロジックを維持）
- [x] 2.5 `makeIntermediateQuestion` を新フローに書き換える: 候補が空なら再抽選（上限10回）、打ち切り時はデッド関連性条件を外してレンジ内かつ残り1コンボ以上のハンドから選ぶ
- [x] 2.6 不要になった `QUERY_RANKS` / `pickRank` / `pickRankPair` と旧フォールバック（`aliveRank` のペア問題）を削除する

## 3. テスト（src/lib/__tests__/questions.test.ts）

- [x] 3.1 既存の中級テストを新仕様で通るように確認・更新する（「高いランクが先」テストは維持）
- [x] 3.2 追加テスト: 自分のハンドをハンドタイプ表記に直すとオープンレンジに含まれる（多数回生成で全件検証）
- [x] 3.3 追加テスト: 出題対象ハンド（handLabel）がオープンレンジに含まれる
- [x] 3.4 追加テスト: 出題対象ハンドのランクがデッドカード（ボード+自分のハンド）に1枚以上含まれ、答えが素のコンボ数（6 / 4 / 12）より小さい
- [x] 3.5 追加テスト: 答えは常に1以上
- [x] 3.6 追加テスト: 答えが `expandHandCombos(handLabel, dead).length` と一致する（正解計算の検算）

## 4. ドキュメントと検証

- [x] 4.1 README.md の中級編の説明を新仕様（ハンドはオープンレンジから選出・出題ハンドは必ずデッドカードが絡む）に更新する
- [x] 4.2 `npm run test` と `npm run build` が通ることを確認する
- [x] 4.3 開発サーバーで中級編を実際にプレイし、デッドカードが絡む問題のみ出題されることを目視確認する
