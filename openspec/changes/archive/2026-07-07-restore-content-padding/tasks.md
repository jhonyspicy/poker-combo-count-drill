## 1. コンテンツ余白の追加

- [x] 1.1 `TopPage.tsx` のルート div に `px-4` を追加する
- [x] 1.2 `PlayPage.tsx` のコンテンツエリア div（`flex-1 flex flex-col`）に `px-4` を追加する（タイマーバー・ヘッダーは対象外）
- [x] 1.3 `ResultPage.tsx` のルート div に `px-4` を追加する
- [x] 1.4 `HowToPage.tsx` のルート div に `px-4` を追加する

## 2. 確認

- [x] 2.1 `npm run build` が通ることを確認する
