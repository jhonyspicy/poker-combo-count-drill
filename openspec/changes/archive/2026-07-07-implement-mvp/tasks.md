## 1. プロジェクト基盤

- [x] 1.1 React Router v7 (HashRouter) で4ルート（`/` / `/play` / `/result` / `/howto`）を設定する
- [x] 1.2 `src/config/gameConfig.ts` を作成し、難易度パラメータ（問題数・基準時間・短縮係数・レベル別出題構成）を定義する
- [x] 1.3 Vitest をインストールし、`npm run test` で実行できる設定を追加する

## 2. コンボ計算ロジック

- [x] 2.1 `src/lib/comboCalculator.ts` にポケットペアのコンボ数計算関数を実装する（デッドカード考慮）
- [x] 2.2 スーテッドのコンボ数計算関数を実装する（残るスーツ数をカウント）
- [x] 2.3 オフスートのコンボ数計算関数を実装する（全体 − スーテッド の導出順）
- [x] 2.4 Vitest でコンボ計算のユニットテストを書く（デッドカードなし・あり・CLAUDE.mdの例題を含む）

## 3. 問題エンジン

- [x] 3.1 `src/lib/questions.ts` に単純問題の固定データ（8種）を定義する
- [x] 3.2 ランダムなボード（フロップ/ターン/リバー）を重複なしで生成する関数を実装する
- [x] 3.3 デッドカード問題を動的生成する関数を実装する（ボード生成 → コンボ計算 → 問題文・解説文生成）
- [x] 3.4 誤答を3つ生成する関数を実装する（デッドカード無視値・近傍値・重複排除・1以上を保証）
- [x] 3.5 レベルと gameConfig に基づいて問題タイプを選択し、1問分のデータ（問題文・4択・正解・解説）を返す関数を実装する

## 4. スコア管理

- [x] 4.1 `src/lib/bestScore.ts` に自己ベストの localStorage 読み書き関数を実装する（型ガード・エラー無視）
- [x] 4.2 セッションスコア（連続正解数・現在レベル）を管理する React カスタムフックを実装する

## 5. 画面実装

- [x] 5.1 トップ画面を実装する（タイトルブロック・自己ベストカード or 案内文・スタートボタン・遊び方リンク）
- [x] 5.2 プレイ画面を実装する（問題文・4択ボタン・タイマーバー）
- [x] 5.3 タイマーを実装する（Date.now() 差分方式・時間切れでゲームオーバー遷移）
- [x] 5.4 正解選択時の次の問題表示と、5問ごとのレベルアップ通知を実装する
- [x] 5.5 リザルト画面を実装する（スコア・問題の解説・自己ベスト表示・更新演出・リトライ/トップボタン）
- [x] 5.6 遊び方画面を実装する（ルール説明・コンボ表チートシート）

## 6. UIポリッシュ

- [x] 6.1 ダーク基調の配色・フォント・余白を Tailwind で整える（モバイルファースト）
- [x] 6.2 タイマー残り時間が少なくなったときの視覚フィードバック（プログレスバーの色変化等）を実装する
- [x] 6.3 `npm run build` が通ることを確認し、GitHub Pages へのデプロイ動作を検証する
