## 1. 実装（完了済み）

- [x] 1.1 `src/pages/TopPage.tsx` のルート要素を `min-h-screen` → `min-h-dvh` に変更
- [x] 1.2 `src/pages/HowToPage.tsx` のルート要素を `min-h-screen` → `min-h-dvh` に変更
- [x] 1.3 `src/pages/ResultPage.tsx` のルート要素を `min-h-screen` → `min-h-dvh` に変更
- [x] 1.4 `src/pages/PlayPage.tsx` が既に `min-h-dvh` であることを確認（変更不要）

## 2. 検証

- [x] 2.1 `npm run build`（型チェック込み）が通ることを確認
- [ ] 2.2 実機のモバイルブラウザで、トップ・リザルト画面がアドレスバー表示中でも余分にスクロールしないことを確認
